      <div class="sharing-card sharing-card-chatroom" data-controller="sharing-card sharing-card-chatroom" data-sharing-card-path-value="<%= sharing_attendees_path(sharing) %>">
        <div class="d-flex h-100">
            <div class="sharing-image h-100">
              <%= cl_image_tag sharing.movie.photo.key, class: "movie-img h-100" if sharing.movie.photo.attached? %>
            </div>

            <div class="d-flex flex-column ps-4 sharing-infos justify-content-center">
              <p class="mb-2"><i class="fa-regular fa-calendar"></i> <strong><%= sharing.session.start_at.strftime("%d/%m")%></strong> à <strong><%=sharing.session.start_at.strftime("%H:%M") %></strong></p>
              <p class="mb-2 border-bottom pb-2"><i class="fa-solid fa-location-dot"></i> <strong><%= sharing.session.cinema.name %></strong></p>
              <p class="sharing-movie-title"><%= sharing.movie.title%></p>
            </div>

              <% unless current_page?(sharing_path(sharing)) %>
                <div class="align-items-center d-flex justify-content-between mt-2 sharing-icon">
                  <p class="align-items-center d-flex font-size-user">
                      <%= link_to sharing_path(sharing) do  %>
                        <i class="fa-regular fa-comment font-size-user"></i>
                      <% end %>
                       <span class="ms-2"></span> <%= sharing.messages.count %>
                  </p>
              </div>
              <% end %>
            </div>
        </div>
